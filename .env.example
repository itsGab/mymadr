# ============================================
# Banco de Dados
# ============================================

# Defina a URL de conexão do banco de dados dependendo do estágio do desenvolvimento:
# - Para desenvolvimento local síncrono ou assíncrono: use SQLite
# - Para ambientes de produção ou containers (assíncrono): use PostgreSQL

DATABASE_URL="mudar dependendo do estágio do desenvolvimento"

# SQLite (para desenvolvimento ou testes rápidos)
# DATABASE_URL="sqlite:///database.db"  # Operações síncronas
# DATABASE_URL="sqlite+aiosqlite:///database.db"  # Operações assíncronas

# PostgreSQL (para desenvolvimento avançado ou produção, após containerização)
# DATABASE_URL="postgresql+psycopg://app_user:app_password@127.0.0.1:5432/app_db"
# ! Lembre-se de alterar as credenciais para produção, como o usuário, senha e nome do banco de dados.
# ! Em produção, use o endereço IP ou o nome do serviço do banco de dados (não "localhost / 127.0.0.1").


# ============================================
# Segurança
# ============================================

# Chave secreta (deve ser gerada de forma segura e única para a sua aplicação)
SECRET_KEY="your-secret-key"  
# ! Lembre-se de gerar uma chave secreta única para produção. Evite deixar a chave fixa no código.
# Uma boa prática é gerar dinamicamente, por exemplo, com: `python -c "import os; print(os.urandom(24))"`

# Algoritmo JWT (usado para criptografar e verificar tokens de autenticação)
ALGORITHM="HS256"

# Tempo de expiração do token (em minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=60  # Ajuste conforme necessário para a sua aplicação. 